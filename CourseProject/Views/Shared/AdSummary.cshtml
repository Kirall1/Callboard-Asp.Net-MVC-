@model Ad
<a class="not-link" href="/Ad/@Model.AdID">
    <div class="card-wrapper">
        <img src="@Model.Photos.Split("|")[0]">

        <div class="details">
            <h4>
                @Model.Name
                <span class="price">$@Model.Price
            </h4>

            <div>City: @Model.City</div>

            <div>Uploaded on: @Model.CreatedDate</div>
        </div>

    </div>
</a>
